# Post-Cutover Operating Notes

The consolidation is now in normal production-maintenance mode. Treat `main` as the canonical application lineage and keep client-specific data/secrets isolated in Railway. Use `PRODUCTION_VALIDATION.md` for releases and preserve rollback references until external acceptance and persistence verification are complete.
